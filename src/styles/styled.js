import styled from "styled-components";

export const MainGComponent = styled.main`
    min-height:100vh;
    gap: 20px;
    background-color: var(--color-grey4);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
`